# IONIC format — version 1

This file is the exact byte layout for IONIC `format_version = 1`. It is the contract:
the reader and the writer must match it byte for byte.

- `format_version` (header byte 9) is `1`. A reader uses this number to pick the spec.
- Status: pre-release (package `0.1.0`). The bytes may still change before the freeze.
- A breaking change creates a new file `spec/v2.md` and sets `format_version = 2`. A
  safe, additive change (a new A0/B0 tail field, a new codec or dtype value) stays
  version 1 and is recorded here.
- For a plain-English overview of how the parts fit together, see
  `crates/parser/src/description.MD`.

All integers are little-endian. The file is seekable; the header is written last.

## Design rules

- Source-independent: the format stores items, arrays, summaries, metadata, and
  offsets into its own file. It knows nothing about where the data came from.
- Metadata is keyed by PSI accession. Structural attributes with no PSI term use
  Ionic's reserved attribute namespace. The accession is the authority, never the
  human name.
- Offsets and lengths in the header are authoritative. Physical order is fixed but a
  reader must trust the header, not adjacency.

## Section map (letters used in the workplan)

| Letter | Role | Shape |
|---|---|---|
| header | file header | fixed 1024 bytes |
| — | spectrum array blocks (`packed_spectra`) | block container, directory at tail |
| — | chromatogram array blocks (`packed_chroms`) | block container, directory at tail |
| A0 | spectrum fast-filter summary (`spec_summary`) | fixed 128-byte record per spectrum |
| A1 | spectrum array index (`spec_entries`) | fixed 16-byte record per spectrum |
| A2 | spectrum array refs (`spec_arrayrefs`) | fixed 32-byte record per array |
| A3 | spectrum segment bounds (optional, core section) | 24-byte row per searchable segment |
| B0 | chromatogram fast-filter summary (`chrom_summary`) | fixed 128-byte record per chromatogram |
| B1 | chromatogram array index (`chrom_entries`) | fixed 16-byte record per chromatogram |
| B2 | chromatogram array refs (`chrom_arrayrefs`) | fixed 32-byte record per array |
| B3 | chromatogram segment bounds (optional, core section) | 24-byte row per searchable segment |
| C | spectrum metadata (`spec_meta`) | grouped, directory at tail |
| D | chromatogram metadata (`chrom_meta`) | grouped, directory at tail |
| E | global metadata (`global_meta`) | one monolithic compressed section |

Physical order: header, packed_spectra, packed_chroms, A0, A1, A2, A3, B0, B1, B2, B3,
C, D, E, then the optional extension area (for future kinds 3+), trailer. Every section
starts on an 8-byte boundary. A0/A1/A2/B0/B1/B2 are never compressed; A3/B3 are
compressed with the file codec when compression is enabled.

## Header (1024 bytes)

| Offset | Size | Field |
|---|---|---|
| 0 | 8 | signature `IONIC\0\0\0` |
| 8 | 1 | endianness flag (0 = little-endian) |
| 9 | 2 | format version (u16, = 1) |
| 11 | 1 | compression codec (0 none, 1 zstd) |
| 12 | 1 | compression level (0–22) |
| 13 | 1 | default array filter (PackingId, see codes) |
| 14 | 2 | reserved, zero |
| 16 | 8 | target block uncompressed size |
| 24 | 8 | off A0 (spec_summary) |
| 32 | 8 | len A0 |
| 40 | 8 | off A1 (spec_entries) |
| 48 | 8 | len A1 |
| 56 | 8 | off A2 (spec_arrayrefs) |
| 64 | 8 | len A2 |
| 72 | 8 | off A3 (spec segment bounds, 0 = none) |
| 80 | 8 | len A3 (stored bytes) |
| 88 | 8 | off B0 (chrom_summary) |
| 96 | 8 | len B0 |
| 104 | 8 | off B1 (chrom_entries) |
| 112 | 8 | len B1 |
| 120 | 8 | off B2 (chrom_arrayrefs) |
| 128 | 8 | len B2 |
| 136 | 8 | off B3 (chrom segment bounds, 0 = none) |
| 144 | 8 | len B3 (stored bytes) |
| 152 | 8 | off C (spec_meta) |
| 160 | 8 | len C |
| 168 | 8 | off D (chrom_meta) |
| 176 | 8 | len D |
| 184 | 8 | off E (global_meta) |
| 192 | 8 | len E |
| 200 | 8 | off packed_spectra |
| 208 | 8 | len packed_spectra |
| 216 | 8 | off packed_chroms |
| 224 | 8 | len packed_chroms |
| 232 | 8 | spectrum block count |
| 240 | 8 | chromatogram block count |
| 248 | 8 | spectrum count |
| 256 | 8 | chromatogram count |
| 264 | 8 | C row count |
| 272 | 8 | C numeric count |
| 280 | 8 | C string count |
| 288 | 8 | D row count |
| 296 | 8 | D numeric count |
| 304 | 8 | D string count |
| 312 | 8 | E row count |
| 320 | 8 | E numeric count |
| 328 | 8 | E string count |
| 336 | 8 | spectrum array type count |
| 344 | 8 | chromatogram array type count |
| 352 | 8 | C uncompressed size |
| 360 | 8 | D uncompressed size |
| 368 | 8 | E uncompressed size |
| 376 | 8 | total file size |
| 384 | 8 | metadata group size (items per group, = 8192) |
| 392 | 8 | C group count |
| 400 | 8 | D group count |
| 408 | 8 | off extension table (0 = none) |
| 416 | 8 | len extension table (0 = none) |
| 424 | 4 | spectrum block directory crc32 |
| 428 | 4 | chromatogram block directory crc32 |
| 432 | 8 | plain_len A3 (uncompressed bytes) |
| 440 | 8 | plain_len B3 (uncompressed bytes) |
| 448 | 4 | A3 crc32 |
| 452 | 4 | B3 crc32 |
| 456 | 552 | reserved, zero (checked; off-limits) |
| 1008 | 4 | C crc32 |
| 1012 | 4 | D crc32 |
| 1016 | 4 | E crc32 |
| 1020 | 4 | header crc32 (over bytes 0..1020) |

## A0 — spectrum fast-filter summary (128 bytes)

| Offset | Size | Field | Type / sentinel | State |
|---|---|---|---|---|
| 0 | 8 | rt_seconds | f64 | used |
| 8 | 8 | base_peak_mz | f64 | used |
| 16 | 8 | selected_ion_mz | f64 | used |
| 24 | 8 | base_peak_int | f64 | used |
| 32 | 8 | total_ion_current | f64 | used |
| 40 | 1 | ms_level | u8 | used |
| 41 | 1 | polarity | u8 (0 unknown, 1 positive, 2 negative) | used |
| 42 | 4 | position_x | u32, 0 = unknown | used |
| 46 | 4 | position_y | u32, 0 = unknown | used |
| 50 | 4 | position_z | u32, 0 = unknown | used |
| 54 | 74 | tail | zero | free |

## B0 — chromatogram fast-filter summary (128 bytes)

| Offset | Size | Field | Type / sentinel | State |
|---|---|---|---|---|
| 0 | 8 | lowest_mz | f64 | used |
| 8 | 8 | highest_mz | f64 | used |
| 16 | 8 | lowest_wavelength | f64 | used |
| 24 | 8 | highest_wavelength | f64 | used |
| 32 | 8 | lowest_ion_mobility | f64 | used |
| 40 | 8 | highest_ion_mobility | f64 | used |
| 48 | 1 | polarity | u8 | used |
| 49 | 79 | tail | zero | free |

Proposed future fields, not yet written and not yet locked to an offset:
`precursor_mz`, `product_mz` (both f64, NaN = unknown). They become `used` only when
the writer fills them and this table assigns them an offset.

A0 and B0 are a pure acceleration cache: every used field is derived from the item's
metadata and the same value is stored in C/D under its PSI accession. A0/B0 can be
rebuilt from C/D. There is no per-record checksum, and the header crc does not cover
A0/B0.

## A1 / B1 — array index (16 bytes per item)

| Offset | Size | Field |
|---|---|---|
| 0 | 8 | first array ref index (into A2/B2) |
| 8 | 8 | array ref count |

## A2 / B2 — array refs (32 bytes per array)

| Offset | Size | Field |
|---|---|---|
| 0 | 8 | element offset (inside the block) |
| 8 | 8 | element count |
| 16 | 4 | block id (into the container directory) |
| 20 | 4 | array accession |
| 24 | 1 | dtype (see codes) |
| 25 | 1 | array filter (PackingId, see codes) |
| 26 | 4 | encoded length (0 = fixed-width; >0 = variable-length byte count) |
| 30 | 1 | continues_previous_segment (0 = new array, 1 = next segment of the array above) |
| 31 | 1 | reserved, zero |

Many refs may point at the same block id and element offset; identical arrays may be
shared. The reader does not care whether arrays are shared.

One logical array may be stored as several refs in a row: the first has
`continues_previous_segment = 0`, each following segment has `continues_previous_segment = 1`. To rebuild
the whole array, read the segments in order and join them. A leading `1`, a value above
`1`, or a segment whose dtype or filter differs from the first is an error. All segments of
one array share the same accession, dtype, and filter.

## Array containers (packed_spectra, packed_chroms)

Layout: `[block][block]…[block][block directory]`. Blocks are grouped by element
stride (1, 2, 4, 8 bytes); a block fills up to the target block size, then seals. An
array larger than the target gets its own dedicated block. Each block is compressed
independently when compression is on.

A large fixed-width spectrum array may be cut into ordered segments, each its own block,
written one A2 ref after another (see `continues_previous`). The writer cuts an array
only when it is at least `min_split_bytes` (512 KB), aiming for `target_segment_bytes`
(128 KB) per segment. These two sizes are writer choices, not part of the file: a reader
joins whatever segments it finds and never needs the numbers.

Block directory entry (32 bytes), one per block:

| Offset | Size | Field |
|---|---|---|
| 0 | 8 | payload offset (relative to container start) |
| 8 | 8 | payload size |
| 16 | 8 | uncompressed length |
| 24 | 4 | crc32 of the stored payload |
| 28 | 4 | reserved, zero |

Each entry carries its own payload crc32, but the entry's offset and size are not
covered by it. The whole directory is therefore protected by one crc32 over all its
bytes, stored in the header (392 for spectra, 396 for chromatograms). A reader checks
this before it trusts any offset, so a corrupted offset cannot silently resolve to a
different valid block. An empty container has a zero-length directory and a crc32 of `0`.

## Extension table

This is the only place the format may grow without a new version. The header points to
it (off/len at 376/384); both zero means there is no table and the file has no
extensions. An old reader that does not know an entry just skips it.

The table starts with an 8-byte header, then one fixed record per entry:

Table header (8 bytes):

| Offset | Size | Field |
|---|---|---|
| 0 | 4 | record count |
| 4 | 4 | reserved, zero |

Record (40 bytes):

| Offset | Size | Field |
|---|---|---|
| 0 | 4 | kind (see reserved kinds list) |
| 4 | 4 | reserved, zero |
| 8 | 8 | payload offset (from file start) |
| 16 | 8 | stored length (bytes on disk) |
| 24 | 8 | plain length (bytes after decompress) |
| 32 | 4 | crc32 of the stored payload |
| 36 | 4 | reserved, zero |

A payload is compressed with the file codec when compression is on; then `stored
length` is the compressed size and `plain length` is the size after decompress. With
compression off the two are equal. A reader checks the crc32 before trusting a payload.

Reserved kinds (never reused): `1` A3 spectrum segment bounds (retired — now a core section),
`2` B3 chromatogram segment bounds (retired — now a core section), `3` imaging spatial index,
`4` RT×m/z tiled index. New extensions use kind 5 and up.

## A3 — spectrum segment bounds

A3 is the fast-screening section that accelerates axis-aligned range queries in spectra.
The header points to it (off/len at 400/408); both zero means the section is absent.
When present, A3 is a list of 24-byte rows, sorted by `array_ref_index` ascending:

| Offset | Size | Field |
|---|---|---|
| 0 | 8 | array_ref_index (which A2 ref this row describes) |
| 8 | 8 | low (f64, true minimum of values in that segment) |
| 16 | 8 | high (f64, true maximum of values in that segment) |

`low` and `high` are the **true min and max** of the values **as they are stored** in the
block, not of the original f64 input. So for an array stored as f32, `low` and `high` are
the min and max of the f32 values as decoded. This keeps the bounds exact to what a reader
will retrieve. One row exists per searchable segment, not per array. "Searchable" means an
axis a reader filters on — m/z (MS:1000514) by default, but also drift time, raw ion
mobility, or any other array accession that monotonically increases across the array. The
writer adds rows only when that axis goes up in order; unsorted or intensity arrays get no
rows.

To read values in `[low, high]`: for each segment, keep it if `segment_low <= query_high` and
`segment_high >= query_low`. Decode only kept segments and cut each to range. The requested
axis is identified by A2 `array_type` accession. If A3 is missing, its crc fails, a row
points out of range, the length is not a multiple of 24, or keys are not ascending, the
reader ignores A3 and reads the whole array instead. The answer is identical either way;
only speed differs. A3 is compressed with the file codec when compression is on.

## B3 — chromatogram segment bounds

B3 screens chromatograms by their array axis (typically time). It has the same 24-byte row
layout as A3: `array_ref_index` points into B2 (chromatogram array refs), and `low`/`high`
are the true min/max of stored values in each segment. The header points to B3 (off/len
at 416/424); both zero means the section is absent or the file has no chromatograms. B3
uses the same screening contract as A3: a reader falls back to full-array read if B3 is
absent, fails its crc, or has invalid structure. B3 is compressed with the file codec when
compression is on.

## C / D — grouped metadata

Layout: `[group payload]…[group payload][group directory]`. Items are split into
fixed groups of `metadata group size` (8192) items. Each group is serialized, then
compressed independently when compression is on. A group's value pools are local: no
pool crosses a group boundary. **Metadata node IDs are also group-local**: each group
has its own node-ID space, with the list root at id `1` and item nodes starting at `2`.

Group directory entry (32 bytes), one per group:

| Offset | Size | Field |
|---|---|---|
| 0 | 8 | payload offset (relative to section start) |
| 8 | 8 | payload size |
| 16 | 8 | uncompressed size |
| 24 | 4 | crc32 of the stored payload |
| 28 | 4 | reserved, zero |

Decompressed group body. First a 12-byte header:

| Offset | Size | Field |
|---|---|---|
| 0 | 4 | meta_count (rows in this group) |
| 4 | 4 | numeric_count |
| 8 | 4 | string_count |

Then column arrays in this order (let `n = meta_count`, `g = items in this group`):

| Column | Type | Length | Meaning |
|---|---|---|---|
| `CI` local index offsets | u32 | g + 1 | `CI[i]`..`CI[i+1]` is the row slice for item `i` |
| `MOI` ids | u32 | n | owner node id — which element owns this row |
| `MPI` parent ids | u32 | n | parent node id — used with `MOI` to rebuild the element tree |
| `MTI` tag ids | u8 | n | tag id — which kind of element the row sits in (spectrum, scan, precursor, …) |
| `MRI` CV ref codes | u8 | n | CV vocabulary: `0` MS, `1` UO, `2` NCIT, `3` PEFF, `4` ATTR, `5` IMS, `255` none |
| `MAN` CV accession numbers | u32 | n | numeric tail of the CV term (e.g. `1000285` for `MS:1000285`) |
| `MURI` unit CV ref codes | u8 | n | CV vocabulary for the unit, same codes as `MRI` |
| `MUAN` unit CV accession numbers | u32 | n | numeric tail of the unit CV term |
| `VK` value kinds | u8 | n | `0` numeric, `1` string, `2` empty |
| `VI` value indices | u32 | n | index into the pool selected by `VK` |
| `VN` numeric values | f64 | numeric_count | pool of all f64 values; a numeric row reads `VN[VI]` |
| `VOFF` string offsets | u32 | string_count | byte start of each string in `VS` |
| `VLEN` string lengths | u32 | string_count | byte length of each string in `VS` |
| `VS` string bytes | u8 | sum of string lengths | concatenated UTF-8 bytes; a string row reads `VS[VOFF[VI] : VOFF[VI]+VLEN[VI]]` |

The term name (e.g. "total ion current") is never stored. A reader gets it from the
vocabulary at runtime using `MRI` and `MAN`. The `ATTR` vocabulary (ref code `4`) is
IONIC's own — it covers structural attributes with no PSI term (id, ref, index, count).
Its exact accession numbers are defined in `attr_meta.rs`.

## E — global metadata (monolithic)

One compressed section. It does not scale with item count, so it is not grouped. It
starts with a 32-byte counts header, then the same column arrays as a group body
(but for the whole section, with `index offsets` of length `items + 1`).

Counts header (32 bytes): nine u16 values in this order — file descriptions, ref
param groups, samples, instrument configs, software, data processing, acquisition
settings, cvs, runs — then 14 reserved zero bytes.

## Codes

Compression codec (header byte 11): `0` none, `1` zstd.

dtype (A2/B2 byte 24): `1` f64, `2` f32, `3` f16, `4` i16, `5` i32, `6` i64.

Array filter / PackingId (header byte 13, A2/B2 byte 25): `0` raw, `1` byte shuffle,
`2` delta shuffle.

## Integrity and validation

- The file starts with the signature `IONIC\0\0\0` and ends with the trailer
  `\0\0\0CINOI`.
- The header crc32 at offset 1020 covers header bytes 0..1020.
- C, D, E each have a crc32 in the header over their whole section.
- Every block and every group carries its own crc32.
- Each block container directory carries one crc32 over all its bytes, stored in the
  header (392 for spectra, 396 for chromatograms). The reader checks it on open, before
  it trusts any block offset.
- A0, A1, A2, B0, B1, B2 have no crc32; the reader checks them by structure (bounds,
  alignment, non-overlap), not by checksum.
- A3 and B3 each carry a crc32 in the header (432 and 436); a failed check makes the
  reader drop that section and fall back, never error.
- The extension table has no crc of its own: the header crc32 covers its offset and
  length, and the reader checks the table by structure (counts, bounds, reserved zeros).
  Any future extension payloads carry their own crc32 in their record.
- `total file size` equals the real file length.
- Sections do not overlap, are 8-byte aligned, and sit after the header.

Validation modes:

- Open: cheap directory checks only — directory bounds, each payload ends strictly
  before its directory, `sum(group uncompressed size)` equals the header total, and each
  block container directory matches its header crc32. No decompression.
- Random access: decode and checksum only the target group and the target item's
  blocks.
- Full read / strict: decode all groups and assert the full row, numeric, and string
  totals against the header.

## v1 limits

- Header item counts (spectra, chromatograms): u64.
- Metadata node ids are group-local: each 8192-item group restarts its node ids from a
  fixed base, so the u32 node-id space never accumulates across the file. Item count is
  bounded only by the u64 header counts, not by the node-id width.
- Rows, numeric, string counts per section: u32.
- Global counts: u16 (max 65535 each).
- String pool offsets: u32 (max 4 GiB per section).

## Extensibility (the only allowed growth)

Version 1 grows in two ways, both additive and both safe for an old reader.

**A0/B0 tail fields.** The reserved tail of A0 and B0 may gain optional fields. Rules:

- A0 and B0 stay exactly 128 bytes forever.
- Each tail slot, once assigned an offset, keeps that offset, type, sentinel, unit,
  and canonical metadata accession forever, and is never reused for another meaning.
- A slot is used, reserved, or free (zero).
- Readers ignore slots they do not know. Writers zero any tail bytes they do not set.

**Extension table.** New optional sections (such as A3) are added as new `kind` values
in the extension table. Rules:

- A `kind` number, once assigned, keeps its meaning and layout forever.
- An extension is optional: a file without it is still valid, and a reader skips any
  `kind` it does not know.
- An extension is droppable: it never holds the only copy of anything needed to read the
  data, so a reader may always fall back without it.

The header reserved area at 392..1008 stays checked-for-zero and is off-limits. Anything
there, or any change to a fixed layout above, is a real format change (a new
`format_version` and a new `spec/vN.md`).
