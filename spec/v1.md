# IONIC format — version 1

This file is the exact byte layout for IONIC `format_version = 1`. It is the contract:
the reader and the writer must match it byte for byte.

- `format_version` (header byte 9) is `1`. A reader uses this number to pick the spec.
- Status: pre-release (package `0.1.0`). The bytes may still change before the freeze.
- A breaking change (new codec id, new dtype value, new section layout) creates a new
  file `spec/v2.md` and sets `format_version = 2`. The reader fail-hards on unknown
  codec or dtype values; encountering one means the file was written by a newer version
  and the reader must upgrade.

All integers are little-endian. The file is seekable; the header is written last.

## Design rules

- Source-independent: the format stores items, arrays, summaries, metadata, and
  offsets into its own file. It knows nothing about where the data came from.
- Metadata is keyed by PSI accession. Structural attributes with no PSI term use
  Ionic's reserved attribute namespace. The accession is the authority, never the
  human name.
- Offsets and lengths in the header are authoritative. Physical order is fixed but a
  reader must trust the header, not adjacency.

## Section map

| Letter | Role                                                       | Shape                                  |
| ------ | ---------------------------------------------------------- | -------------------------------------- |
| header | file header                                                | fixed 1024 bytes                       |
| —      | spectrum array blocks (`packed_spectra`)                   | block container, directory at tail     |
| —      | chromatogram array blocks (`packed_chroms`)                | block container, directory at tail     |
| A0     | spectrum m/z-window directory — window → A3 addresses      | u32 columns (CSR), compressed          |
| A1     | spectrum fast-filter summary (`spec_summary`)              | fixed 128-byte record per spectrum     |
| A2     | spectrum array index (`spec_entries`)                      | fixed 16-byte record per spectrum      |
| A3     | spectrum array address table (`spec_array_addresses`)      | fixed 32-byte record per array         |
| B0     | chromatogram window directory — window → B3 addresses      | u32 columns (CSR), compressed          |
| B1     | chromatogram fast-filter summary (`chrom_summary`)         | fixed 128-byte record per chromatogram |
| B2     | chromatogram array index (`chrom_entries`)                 | fixed 16-byte record per chromatogram  |
| B3     | chromatogram array address table (`chrom_array_addresses`) | fixed 32-byte record per array         |
| C      | spectrum metadata (`spec_meta`)                            | grouped, directory at tail             |
| D      | chromatogram metadata (`chrom_meta`)                       | grouped, directory at tail             |
| E      | global metadata (`global_meta`)                            | one monolithic compressed section      |

A1+A2+A3 are the per-spectrum tables (summary, index, addresses): read in order to
reconstruct one spectrum (`A1 → A2 → A3`). A0 is the m/z-window directory — the
cross-spectrum index an extracted-ion read enters first, then dips into A1 for rt and A3
for blocks (`A0 → A1 → A3`).

Physical order: header, packed_spectra, packed_chroms, A1, A2, A3, B1, B2, B3, C, D, E,
A0, B0, trailer. The header is authoritative; a reader trusts its offsets, not adjacency.
Every section starts on an 8-byte boundary. A1/A2/A3/B1/B2/B3 are never compressed; A0/B0
are compressed with the file codec when compression is enabled.

## Header (1024 bytes)

| Offset | Size | Field                                           |
| ------ | ---- | ----------------------------------------------- |
| 0      | 8    | signature `IONIC\0\0\0`                         |
| 8      | 1    | endianness flag (0 = little-endian)             |
| 9      | 2    | format version (u16, = 1)                       |
| 11     | 1    | compression codec (0 none, 1 zstd)              |
| 12     | 1    | compression level (0–22)                        |
| 13     | 1    | default array filter (PackingId, see codes)     |
| 14     | 2    | reserved, zero                                  |
| 16     | 8    | target block uncompressed size (u64)            |
| 24     | 4    | target m/z window (u32, m/z units; default 100) |
| 28     | 4    | reserved, zero                                  |
| 32     | 8    | off A0 (spec window directory)                  |
| 40     | 8    | len A0 (stored bytes)                           |
| 48     | 8    | off A1 (spec_summary)                           |
| 56     | 8    | len A1                                          |
| 64     | 8    | off A2 (spec_entries)                           |
| 72     | 8    | len A2                                          |
| 80     | 8    | off A3 (spec_array_addresses)                   |
| 88     | 8    | len A3                                          |
| 96     | 8    | off B0 (chrom window directory)                 |
| 104    | 8    | len B0 (stored bytes)                           |
| 112    | 8    | off B1 (chrom_summary)                          |
| 120    | 8    | len B1                                          |
| 128    | 8    | off B2 (chrom_entries)                          |
| 136    | 8    | len B2                                          |
| 144    | 8    | off B3 (chrom_array_addresses)                  |
| 152    | 8    | len B3                                          |
| 160    | 8    | off C (spec_meta)                               |
| 168    | 8    | len C                                           |
| 176    | 8    | off D (chrom_meta)                              |
| 184    | 8    | len D                                           |
| 192    | 8    | off E (global_meta)                             |
| 200    | 8    | len E                                           |
| 208    | 8    | off packed_spectra                              |
| 216    | 8    | len packed_spectra                              |
| 224    | 8    | off packed_chroms                               |
| 232    | 8    | len packed_chroms                               |
| 240    | 8    | spectrum block count (u64)                      |
| 248    | 8    | chromatogram block count (u64)                  |
| 256    | 8    | spectrum count (u64)                            |
| 264    | 8    | chromatogram count (u64)                        |
| 272    | 8    | spectrum array type count (u64)                 |
| 280    | 8    | chromatogram array type count (u64)             |
| 288    | 8    | C row count (u64)                               |
| 296    | 8    | C numeric count (u64)                           |
| 304    | 8    | C string count (u64)                            |
| 312    | 8    | D row count (u64)                               |
| 320    | 8    | D numeric count (u64)                           |
| 328    | 8    | D string count (u64)                            |
| 336    | 8    | E row count (u64)                               |
| 344    | 8    | E numeric count (u64)                           |
| 352    | 8    | E string count (u64)                            |
| 360    | 8    | C uncompressed size (u64)                       |
| 368    | 8    | D uncompressed size (u64)                       |
| 376    | 8    | E uncompressed size (u64)                       |
| 384    | 8    | plain_len A0 (uncompressed bytes, u64)          |
| 392    | 8    | plain_len B0 (uncompressed bytes, u64)          |
| 400    | 8    | total file size (u64)                           |
| 408    | 8    | metadata group size (items per group, u64)      |
| 416    | 8    | C group count (u64)                             |
| 424    | 8    | D group count (u64)                             |
| 432    | 536  | reserved, zero (checked; off-limits)            |
| 968    | 4    | A0 crc32 (spec window directory)                |
| 972    | 4    | A1 crc32 (spec_summary)                         |
| 976    | 4    | A2 crc32 (spec_entries)                         |
| 980    | 4    | A3 crc32 (spec_array_addresses)                 |
| 984    | 4    | B0 crc32 (chrom window directory)               |
| 988    | 4    | B1 crc32 (chrom_summary)                        |
| 992    | 4    | B2 crc32 (chrom_entries)                        |
| 996    | 4    | B3 crc32 (chrom_array_addresses)                |
| 1000   | 4    | spectrum block directory crc32                  |
| 1004   | 4    | chromatogram block directory crc32              |
| 1008   | 4    | C crc32 (spec_meta)                             |
| 1012   | 4    | D crc32 (chrom_meta)                            |
| 1016   | 4    | E crc32 (global_meta)                           |
| 1020   | 4    | header crc32 (over bytes 0..1020)               |

## A0 — spectrum m/z-window directory

A0 is the m/z column index: it turns an m/z value into the segments that hold it, so an
extracted-ion read (one m/z window across all spectra) opens only those blocks instead of
scanning every spectrum. A window is `floor(mz / target_mz_window)` (width at header offset
24). The header points to A0 (off/len at 32/40) and carries a CRC32 for it at offset 968.
A0 is compressed with the file codec when compression is on; the CRC32 covers the stored
(compressed) bytes.

A0 holds **pointers only** — A3 stays the single source of truth for block, offset and
dtype. The body is monotonic `u32` arrays stored column-wise (CSR, exactly like A2→A3):

| Part              | Type | Length           | Meaning                                               |
| ----------------- | ---- | ---------------- | ----------------------------------------------------- |
| window_count      | u32  | 1                | number of windows (`starts` has window_count+1 slots) |
| entry_count       | u32  | 1                | number of window entries                              |
| starts            | u32  | window_count + 1 | `starts[b]..starts[b+1]` is window b's entry slice    |
| spectrum_index    | u32  | entry_count      | owning spectrum, strictly ascending within a window   |
| mz_address        | u32  | entry_count      | A3 row of that window+spectrum's m/z segment          |
| intensity_address | u32  | entry_count      | A3 row of the paired intensity segment                |

One entry exists per (window, spectrum) that stores m/z in that window. Each column climbs within
a window, so the file codec compresses it tightly. An empty A0 (no windowed m/z arrays) is a
zero-length section. `rt` is never stored here — a reader reads it from `A1[spectrum_index]`.

**A0 is required for m/z range and extracted-ion reads. There is no full-array fallback.**
The writer guarantees A0 exists by rejecting any spectrum whose m/z array is not sorted
ascending. A reader checks the CRC32, then validates structure (`starts` runs `0 → entry_count`
and never decreases, `spectrum_index` strictly increases within a window, every address is in
range); a missing, CRC-failed or malformed A0 is an error, not a silent wrong answer (re-encode
the file from its mzML source with `ionic convert`).

To read `[query_low, query_high]`: for each window in
`floor(query_low/width)..=floor(query_high/width)`, take its entry slice, read the m/z and
intensity A3 rows, decode those blocks, and cut to range. Finite rt is stored in ascending
order (the writer rejects any spectrum whose rt drops below the previous, just as it rejects
unsorted m/z), so when every spectrum has a finite rt a window's entry slice is rt-ascending in
`spectrum_index` and an rt range is found by **binary search** over `A1[spectrum_index].rt`. A
spectrum may legitimately lack rt (non-finite, e.g. SRM); that disables the binary-search fast
path for the file and the reader scans each window's slice instead. The window width is an
integer number of m/z units (header offset 24); the writer rounds it so the reader recomputes
the same window a value was stored in.

## A1 — spectrum fast-filter summary (128 bytes)

A1 is a rebuildable acceleration cache: every used field is derived from the spectrum's
metadata in C under its PSI accession. A1 can be rebuilt from C. The header carries a
CRC32 for A1 at offset 972.

| Offset | Size | Field             | Type / sentinel                                   | State |
| ------ | ---- | ----------------- | ------------------------------------------------- | ----- |
| 0      | 8    | rt                | f64, raw value in the unit at byte 54             | used  |
| 8      | 8    | base_peak_mz      | f64                                               | used  |
| 16     | 8    | selected_ion_mz   | f64                                               | used  |
| 24     | 8    | base_peak_int     | f64                                               | used  |
| 32     | 8    | total_ion_current | f64                                               | used  |
| 40     | 1    | ms_level          | u8                                                | used  |
| 41     | 1    | polarity          | u8 (0 unknown, 1 positive, 2 negative)            | used  |
| 42     | 4    | position_x        | u32, 0 = unknown                                  | used  |
| 46     | 4    | position_y        | u32, 0 = unknown                                  | used  |
| 50     | 4    | position_z        | u32, 0 = unknown                                  | used  |
| 54     | 1    | rt_unit           | u8 (0 unknown, 1 second, 2 minute, 3 millisecond) | used  |
| 55     | 73   | tail              | zero                                              | free  |

Finite rt is stored ascending across spectra: the writer rejects a spectrum whose rt drops
below the previous spectrum's, so when every spectrum has a finite rt A0's per-window slices
are rt-ordered and an extracted-ion read can binary-search them. A spectrum may lack rt
(non-finite); that is allowed but disables the rt binary-search fast path for the file.

A1 and B1 stay exactly 128 bytes forever. The free tail may gain optional fields. Each
tail slot, once assigned an offset, keeps that offset, type, sentinel, unit, and
canonical metadata accession forever and is never reused for another meaning. Readers
ignore slots they do not know; writers zero any tail bytes they do not set.

## A2 — spectrum array index (16 bytes per spectrum)

A2 maps each spectrum to its array addresses in A3. The header carries a CRC32 for A2 at
offset 976.

| Offset | Size | Field                         |
| ------ | ---- | ----------------------------- |
| 0      | 8    | first array address (into A3) |
| 8      | 8    | array address count           |

## A3 — spectrum array address table (32 bytes per array)

A3 holds one record per logical array segment. The header carries a CRC32 for A3 at
offset 980.

| Offset | Size | Field                                                                           |
| ------ | ---- | ------------------------------------------------------------------------------- |
| 0      | 8    | element offset (inside the block)                                               |
| 8      | 8    | element count (number of elements in this segment)                              |
| 16     | 4    | block id (into the container directory)                                         |
| 20     | 4    | array accession (numeric tail of the CV term)                                   |
| 24     | 1    | dtype (see codes)                                                               |
| 25     | 1    | array filter (PackingId, see codes)                                             |
| 26     | 4    | encoded length (0 = fixed-width; >0 = variable-length byte count)               |
| 30     | 1    | continues_previous_segment (0 = new array, 1 = next segment of the array above) |
| 31     | 1    | array accession CV ref code (same codes as C/D `MRI`: 0 MS, 1 UO, …, 255 none)  |

The accession at byte 20 is only the numeric tail of the CV term; byte 31 names its
vocabulary, so the array type is fully qualified without assuming MS. All segments of one
array share the same CV ref code.

Window membership is not stored per segment: the window → segment mapping lives only in the
A0 window directory (the inverse index), so a range or extracted-ion read goes through A0, and
a whole-spectrum read goes through `A1 → A2 → A3` without needing it here.

Each address resolves on its own from block id, element offset, and element count. The writer
emits one address per stored segment and does not deduplicate arrays.

One logical array may be stored as several addresses in a row: the first has
`continues_previous_segment = 0`, each following segment has `continues_previous_segment = 1`.
To rebuild the whole array, read the segments in order and join them. A leading `1`, a
value above `1`, or a segment whose dtype or filter differs from the first is an error.
All segments of one array share the same accession, dtype, and filter.

## B0 — chromatogram window directory

B0 is the chromatogram equivalent of A0 and has A0's layout, but chromatograms are not
split by m/z window: every chromatogram is a single entry under window 0. The header
carries a CRC32 for B0 at offset 984.

## B1 — chromatogram fast-filter summary (128 bytes)

B1 is the chromatogram equivalent of A1 — a rebuildable acceleration cache derived from
D. The header carries a CRC32 for B1 at offset 988.

| Offset | Size | Field                | Type / sentinel | State |
| ------ | ---- | -------------------- | --------------- | ----- |
| 0      | 8    | lowest_mz            | f64             | used  |
| 8      | 8    | highest_mz           | f64             | used  |
| 16     | 8    | lowest_wavelength    | f64             | used  |
| 24     | 8    | highest_wavelength   | f64             | used  |
| 32     | 8    | lowest_ion_mobility  | f64             | used  |
| 40     | 8    | highest_ion_mobility | f64             | used  |
| 48     | 1    | polarity             | u8              | used  |
| 49     | 79   | tail                 | zero            | free  |

Proposed future fields, not yet written and not yet locked to an offset:
`precursor_mz`, `product_mz` (both f64, NaN = unknown). They become `used` only when
the writer fills them and this table assigns them an offset.

B2 (16 bytes per chromatogram) and B3 (32 bytes per array) are the chromatogram equivalents
of A2 and A3 and mirror them exactly.

## Array containers (packed_spectra, packed_chroms)

Layout: `[block][block]…[block][block directory]`. Blocks are grouped by array type,
element stride (1, 2, 4, 8 bytes), and m/z window; a block fills to the target block size,
then seals. An array larger than the target gets its own dedicated block. Each block is
compressed independently when compression is on.

A spectrum's m/z and intensity arrays are cut into ordered **m/z windows**: the m/z axis is
split into fixed-width windows, and each window's slice becomes one A3 segment (the first has
`continues_previous_segment = 0`, each later window `1`). The window index of an m/z value is
`floor(mz / width)`, where `width` is the target m/z window at header offset 24 (u32, m/z units;
default 100). Same-window slices from different spectra share blocks (a block holds one array
type, one element stride, one window), so a range read opens only the blocks of the windows it
overlaps. The reader finds them by computing the window indices of its m/z range with the same
`width`, then looking those windows up in A0. The writer rounds `width` to an integer before
storing it, so the reader recomputes the exact window each value was placed in.

Block directory entry (32 bytes), one per block:

| Offset | Size | Field                                        |
| ------ | ---- | -------------------------------------------- |
| 0      | 8    | payload offset (relative to container start) |
| 8      | 8    | payload size                                 |
| 16     | 8    | uncompressed length                          |
| 24     | 4    | crc32 of the stored payload                  |
| 28     | 4    | reserved, zero                               |

Each entry carries its own payload crc32, but the entry's offset and size are not
covered by it. The whole directory is therefore protected by one crc32 over all its
bytes, stored in the header (992 for spectra, 996 for chromatograms). A reader checks
this before it trusts any offset, so a corrupted offset cannot silently resolve to a
different valid block. An empty container has a zero-length directory and a crc32 of `0`.

## C / D — grouped metadata

Layout: `[group payload]…[group payload][group directory]`. Items are split into
fixed groups of `metadata group size` (8192) items. Each group is serialized, then
compressed independently when compression is on. A group's value pools are local: no
pool crosses a group boundary. **Metadata node IDs are also group-local**: each group
has its own node-ID space, with the list root at id `1` and item nodes starting at `2`.

Group directory entry (32 bytes), one per group:

| Offset | Size | Field                                      |
| ------ | ---- | ------------------------------------------ |
| 0      | 8    | payload offset (relative to section start) |
| 8      | 8    | payload size                               |
| 16     | 8    | uncompressed size                          |
| 24     | 4    | crc32 of the stored payload                |
| 28     | 4    | reserved, zero                             |

Decompressed group body. First a 12-byte header:

| Offset | Size | Field                           |
| ------ | ---- | ------------------------------- |
| 0      | 4    | meta_count (rows in this group) |
| 4      | 4    | numeric_count                   |
| 8      | 4    | string_count                    |

Then column arrays in this order (let `n = meta_count`, `g = items in this group`):

| Column                           | Type | Length                | Meaning                                                                          |
| -------------------------------- | ---- | --------------------- | -------------------------------------------------------------------------------- |
| `CI` local index offsets         | u32  | g + 1                 | `CI[i]`..`CI[i+1]` is the row slice for item `i`                                 |
| `MOI` ids                        | u32  | n                     | owner node id — which element owns this row                                      |
| `MPI` parent ids                 | u32  | n                     | parent node id — used with `MOI` to rebuild the element tree                     |
| `MTI` tag ids                    | u8   | n                     | tag id — which kind of element the row sits in (spectrum, scan, precursor, …)    |
| `MRI` CV ref codes               | u8   | n                     | CV vocabulary: `0` MS, `1` UO, `2` NCIT, `3` PEFF, `4` ATTR, `5` IMS, `255` none |
| `MAN` CV accession numbers       | u32  | n                     | numeric tail of the CV term (e.g. `1000285` for `MS:1000285`)                    |
| `MURI` unit CV ref codes         | u8   | n                     | CV vocabulary for the unit, same codes as `MRI`                                  |
| `MUAN` unit CV accession numbers | u32  | n                     | numeric tail of the unit CV term                                                 |
| `VK` value kinds                 | u8   | n                     | `0` numeric, `1` string, `2` empty                                               |
| `VI` value indices               | u32  | n                     | index into the pool selected by `VK`                                             |
| `VN` numeric values              | f64  | numeric_count         | pool of all f64 values; a numeric row reads `VN[VI]`                             |
| `VOFF` string offsets            | u32  | string_count          | byte start of each string in `VS`                                                |
| `VLEN` string lengths            | u32  | string_count          | byte length of each string in `VS`                                               |
| `VS` string bytes                | u8   | sum of string lengths | concatenated UTF-8 bytes; a string row reads `VS[VOFF[VI] : VOFF[VI]+VLEN[VI]]`  |

The term name (e.g. "total ion current") is never stored. A reader gets it from the
vocabulary at runtime using `MRI` and `MAN`. The `ATTR` vocabulary (ref code `4`) is
IONIC's own — it covers structural attributes with no PSI term (id, ref, index, count).
Its exact accession numbers are defined in `attr_meta.rs`.

## E — global metadata (monolithic)

One compressed section. It does not scale with item count, so it is not grouped. It
starts with a 32-byte counts header, then the same column arrays as a group body
(but for the whole section, with `index offsets` of length `items + 1`).

Counts header (32 bytes): nine u16 values in this order — file descriptions, ref
param groups, samples, instrument configs, software, data processing, acquisition
settings, cvs, runs — then 14 reserved zero bytes.

## Codes

Compression codec (header byte 11): `0` none, `1` zstd. An unknown codec id is a
breaking change — the reader fail-hards and requires a new `format_version`.

dtype (A2/B2 byte 24): `1` f64, `2` f32, `3` f16, `4` i16, `5` i32, `6` i64. An
unknown dtype value is a breaking change — the reader fail-hards and requires a new
`format_version`.

Array filter / PackingId (header byte 13, A2/B2 byte 25): `0` raw, `1` byte shuffle,
`2` delta shuffle.

## Integrity and validation

- The file starts with the signature `IONIC\0\0\0` and ends with the trailer
  `\0\0\0CINOI`.
- The header crc32 at offset 1020 covers header bytes 0..1020.
- All 14 section checksums are grouped at the header tail (968..1020): A0 at 968, A1
  at 972, A2 at 976, A3 at 980, B0 at 984, B1 at 988, B2 at 992, B3 at 996, spectrum
  block directory at 1000, chromatogram block directory at 1004, C at 1008, D at 1012,
  E at 1016.
- C, D, E each have a crc32 in the header over their whole section.
- Every block and every group carries its own crc32.
- Each block container directory carries one crc32 over all its bytes, stored in the
  header (1000 for spectra, 1004 for chromatograms). The reader checks it on open, before
  it trusts any block offset.
- A3 and B3 each carry a crc32 in the header (980 and 996). A3 and B3 are required
  for range reads; a failed CRC is an error, not a fallback.
- `total file size` equals the real file length.
- Sections do not overlap, are 8-byte aligned, and sit after the header.
- Reserved bytes at header 14..16 and 432..968 are checked zero on parse; a non-zero
  byte is an error.

Validation modes:

- Open: cheap directory checks only — directory bounds, each payload ends strictly
  before its directory, `sum(group uncompressed size)` equals the header total, and each
  block container directory matches its header crc32. No decompression.
- Random access: decode and checksum only the target group and the target item's
  blocks.
- Full read / strict: decode all groups and assert the full row, numeric, and string
  totals against the header.

## v1 limits

- Header item counts (spectra, chromatograms): u64.
- Metadata node ids are group-local: each 8192-item group restarts its node ids from a
  fixed base, so the u32 node-id space never accumulates across the file. Item count is
  bounded only by the u64 header counts, not by the node-id width.
- Rows, numeric, string counts per section: u32.
- Global counts: u16 (max 65535 each).
- String pool offsets: u32 (max 4 GiB per section).

## Extensibility (the only allowed growth)

Version 1 grows in exactly one way: **A1/B1 tail fields**. The reserved tail of A1 and
B1 (the fast-filter summaries) may gain optional fields. Rules:

- A1 and B1 stay exactly 128 bytes forever.
- Each tail slot, once assigned an offset, keeps that offset, type, sentinel, unit,
  and canonical metadata accession forever, and is never reused for another meaning.
- A slot is used, reserved, or free (zero).
- Readers ignore slots they do not know. Writers zero any tail bytes they do not set.

The header reserved area at 448..992 stays checked-for-zero and is off-limits. Anything
there, or any change to a fixed layout above, is a real format change (a new
`format_version` and a new `spec/vN.md`).
